code:
  local_path: .
command: |
  pip install azureml-defaults scikit-learn numpy inference-schema[numpy-support]
  python train.py
environment:
  docker:
    image: python
compute:
  target: azureml:cpu-cluster
experiment_name: diabetes-example
description: Train a model on the Diabetes dataset.
