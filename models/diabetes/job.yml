code:
  local_path: .
command: |
  pip install azureml-core==1.34.0 scikit-learn numpy
  python train.py
environment:
  docker:
    image: python
compute:
  target: azureml:cpu-cluster
experiment_name: diabetes-example
description: Train a model on the Diabetes dataset.
